services:
  thoughtpad:
    restart: unless-stopped
    build:
      context: .
      dockerfile: server/Dockerfile
    ports:
      - "4000:3000"
    env_file:
      - .env
    volumes:
      - ./server/data:/app/data
